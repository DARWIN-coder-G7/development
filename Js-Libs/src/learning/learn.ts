const map = new Map();

// Add values
map.set("name", "Darwin");
map.set(1, "one");
map.set(true, "boolean key");

// Get values
console.log(map.get("name")); 
console.log(map.get(1));      

// Check if key exists
console.log(map.has(true));   

console.log(map.size);   

// console.log(map.clear());
console.log(map.keys().return);
console.log(map.values().return);


// Function Generators

